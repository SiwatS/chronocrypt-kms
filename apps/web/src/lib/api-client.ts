/**
 * KMS API Client
 *
 * Type-safe client using Elysia Eden Treaty - all types inferred from backend
 */

import { api } from './eden-client';

// Re-export the Eden client for direct use
export { api };

// Export default for convenience
export default api;
