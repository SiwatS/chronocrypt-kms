# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/chronocrypt_kms?schema=public"

# KMS Configuration
PORT=3001
KMS_ID="kms-main"

# Master Key Encryption (for encrypting the private key at rest)
# Generate a strong random key in production
MASTER_KEY_ENCRYPTION_KEY="your-very-secure-encryption-key-here"

# Environment
NODE_ENV="development"

# Optional: Enable detailed Prisma logging
# Uncomment for debugging
# DEBUG="prisma:*"
