# Backend Configuration
PORT=3001

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Database (PostgreSQL with Prisma)
# For development with Docker: docker-compose -f docker-compose.dev.yml up -d
DATABASE_URL=postgresql://chronocrypt:chronocrypt-dev-password@localhost:5432/chronocrypt_kms?schema=public

# Security
# Master key encryption (used to encrypt private keys at rest)
MASTER_KEY_ENCRYPTION_KEY=change-me-in-production-use-strong-random-key
KMS_ID=kms-main
