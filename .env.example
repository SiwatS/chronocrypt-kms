# Backend Configuration
PORT=3001

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Authentication
# IMPORTANT: Change these credentials in production!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=chronocrypt-admin-change-me

# Database (PostgreSQL with Prisma)
DATABASE_URL=postgresql://chronocrypt:chronocrypt_password@localhost:5432/chronocrypt_kms?schema=public

# Security
# Master key encryption (used to encrypt private keys at rest)
MASTER_KEY_ENCRYPTION_KEY=change-me-in-production-use-strong-random-key
KMS_ID=kms-main
